package csimilarity;

import org.junit.Test;

import static org.hamcrest.core.Is.is;
import static org.junit.Assert.assertFalse;
import static org.junit.Assert.assertThat;
import static org.junit.Assert.assertTrue;

public class DocumentTest {

//    @Test
//    public void shouldCreateTheDcoumentForGivenLinesOfText() {
//        String text = "this is the first line.\n This is the second line";
//        Document document = new Document("myfirstdoc", text);
//        assertThat(document.getWordCount("this"), is(2));
//        assertThat(document.getWordCount("the"), is(2));
//        assertThat(document.getWordCount("shanu"), is(0));
//    }
//
//    @Test
//    public void shouldReturnTrueIfDocumentContainsWord() {
//        String text = "this is the first line.\n This is the second line";
//        Document document = new Document("myfirstdoc", text);
//        assertTrue(document.contains("this"));
//        assertFalse(document.contains("shanu"));
//    }
}